#mainnav {
background-color: {{$theme->back_color}};
}

.navbar-header::before {
background-color: {{$theme->back_color}};
}

body {
    font-family: "{{$theme->font}}", Helvetica, Arial, sans-serif;
}